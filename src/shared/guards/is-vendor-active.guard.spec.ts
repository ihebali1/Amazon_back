import { IsVendorActiveGuard } from './is-vendor-active.guard';

describe('IsVendorActiveGuard', () => {
  it('should be defined', () => {
    expect(new IsVendorActiveGuard()).toBeDefined();
  });
});
